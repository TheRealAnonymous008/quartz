* $\text{Hom}(X,Y)$ - the set of morphisms (functions) with domain $X$ and codomain $Y$.
* $\text{Hom}(X,f)$ - the hom functor which maps $g\mapsto f\circ g$
* $\text{Hom}(g,Y)$ - the hom functor which maps $g\mapsto g\circ h$. 